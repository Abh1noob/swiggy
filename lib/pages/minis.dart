import 'package:flutter/material.dart';

class Minis extends StatelessWidget {
  const Minis({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Minis Page' ,
    );
  }
}